
```rust


{
  create: {
    new: {
      space: {
        name: "label"
      }
    }
  }
}

create a new space
create a new space called "main"
create a new space called "browser" on this display

label default display "main"
label left display "left"
label right display "right"

label this display "main"
label this space "term"
label this window "term"

focus main display
focus browser display
focus main space
focus term space

move this window left
move term window to "main" display
move browser window to "browser" space

swap with next space
swap with prev space
swap this window with "term"
swap top window with bottom
swap left window with right
swap top left window with bottom right

this|name|direction|"a new" - <display|space|window>

swap|move|focus|label - <action>

with|to|called|on
```

yabai -m display|window|space

